version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

python:
  install:
    - requirements: ./requirements.txt

sphinx:
  configuration: ./source/conf.py

# i18n: RTD will detect the language from this project's settings.
# Create a separate RTD project for zh_CN translation, linking it
# as a translation of the main (English) project in RTD admin panel.
